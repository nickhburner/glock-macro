# Python
__pycache__/
*.pyc

# Build output (the exe ships as a GitHub Release asset, not in git)
build/
dist/
*.spec
# ...except the curated main-exe spec build.bat uses (carries the cv2 trim)
!A2 Macro Controller.spec

# Diagnostic captures
debug_capture.png
debug_fullscreen.png
debug_annotated.png

# Raw asset source (only needed to re-run flatten_backgrounds.py)
skills_raw/

# Per-machine settings; ship settings.example.json instead
settings.json

# Per-machine custom button captures (user data, preserved by the updater)
ref/custom/

# Per-machine localized ref captures from the GUI language ref wizard
# (user data, preserved by the updater, never shipped)
ref/fr/
ref/de/

# Old release archives kept locally
*.zip

# Dev screenshots / test images
*.png.bak
2 skill.png
3 skill.png

# fastinput_check.py diagnostic output
_fastcheck/

# Remote-control runtime files (per-machine; remote_token.txt is a SECRET)
remote_token.txt
remote_status.json
remote_cmd.json
remote_heartbeat.json
remote_agent.log
remote_agent.log.1
